const playlist = {
  artistnames: 'song titles'
};

function updatePlaylist(playlist, artistName, songTitle) {
   playlist[artistName]= 'songTitle'
  return updatePlaylist(playlist, artistName, songTitle)
}

